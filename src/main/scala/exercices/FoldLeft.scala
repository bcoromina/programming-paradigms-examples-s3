package exercices

// Implement a partition function using foldLeft. 
// The partition function takes a list of elements and a predicate, and returns a tuple of two lists: 
// the first list contains the elements that satisfy the predicate, and the second list contains the elements that do not satisfy the predicate.